</br><p><%= link_to " Back to all Lists",root_path, class: 'glyphicon glyphicon-circle-arrow-left' %></p>
<div class="col-md-8">
<h2><%=@list.name%></h2>
<div class='js-items-count'><h4><%= pluralize(@list.items.count, 'item')%></h4></div> 
<p><%=@list.description%></p>
<h4><%= link_to "Edit Description", edit_list_path, class: 'btn btn-success' %>
<%= link_to "Delete List", @list, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this topic?' } %> </h4>

</br>
</br>
  <div class='js-items'>
    <%= render partial: 'items/item', collection: @items %>
   </div>

</br>

  <div class='new-item'>
   <%= render partial: 'items/form', locals: { list:@list, item: @item} %>
  </div>
   </div>
